apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "live-streaming-platform.fullname" . }}-config
data:
{{- range $key, $value := .Values.global.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
