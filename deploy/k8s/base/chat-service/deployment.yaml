apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
        - name: chat-service
          image: live-streaming-platform/chat-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          env:
            - name: HTTP_PORT
              value: "8083"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8083
          livenessProbe:
            httpGet:
              path: /livez
              port: 8083
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
spec:
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
